# Root .env.example - Copy this to backend/.env and frontend/.env separately

# ========================================
# BACKEND Environment Variables (backend/.env)
# ========================================
PORT=3001
NODE_ENV=development

# PostgreSQL Database - Works with any PostgreSQL 14+
# Local: postgresql://postgres:postgres@localhost:5432/socsa
# Railway: railway.app provides connection string
# Neon: neon.tech provides connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/socsa

# JWT Configuration - Use strong random string in production
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# Frontend URL for CORS (must match exactly)
FRONTEND_URL=http://localhost:5173

# ========================================
# FRONTEND Environment Variables (frontend/.env)
# ========================================
# Backend API URL
VITE_API_URL=http://localhost:3001
